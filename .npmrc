# pnpm run 前不重新校验依赖树。
# 项目 root 依赖 deepseek-harness 闭包，source 模式下闭包引用未发布的 @deepseek-ai/* 版本，
# 预校验会 ERR_PNPM_NO_MATCHING_VERSION 拦下所有 pnpm run。宿主依赖由 package-sidecar 用 npm 装，
# pnpm 预校验对本项目无意义，关闭。
verify-deps-before-run=false
